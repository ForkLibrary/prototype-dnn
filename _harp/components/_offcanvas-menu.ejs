<% if(offcanvas == true) { %>

  <% if(tabBar == false) { %>
  <% } else { %>
    <div class="tab-bar">
  <% } %>

  <a role="button" class="<% if(position = "left") { %> left-off-canvas-toggle <% } else { %> right-off-canvas-toggle <% } %> menu-icon" href="#" ><span></span> Menu</a>

  <% if(tabBar == false) { %>
  <% } else { %>
    </div>
  <% } %>

<% } %>
